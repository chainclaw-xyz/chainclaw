{
  "name": "@chainclaw/skills-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@chainclaw/core": "*",
    "@chainclaw/skills": "*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
